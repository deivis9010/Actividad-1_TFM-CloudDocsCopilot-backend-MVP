# Configuración para tests
# Copia este archivo como .env.test y ajusta los valores según tu entorno

# Base de datos de prueba (se limpia después de cada test)
TEST_MONGO_URI=mongodb://127.0.0.1:27017/clouddocs-test

# Puerto para el servidor de tests (opcional)
PORT=4001

# JWT
JWT_SECRET=test-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# Bcrypt
BCRYPT_SALT_ROUNDS=10

# Upload (para tests de documentos)
MAX_UPLOAD_SIZE=5242880
ALLOWED_MIME_TYPES=application/pdf,image/png,image/jpeg,text/plain
